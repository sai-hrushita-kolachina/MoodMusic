services:
  - type: web
    name: emotion-music-app
    env: python
    region: oregon
    pythonVersion: "3.13.4"
    buildCommand: |
      pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: |
      gunicorn emotion_service:app
    autoDeploy: true
